					// for (var m in kickChannelMembers) {
					// 	var member = {userID : kickChannelMembers[m].user_id};
					// 	bot.getUser(member, addVoteMembers);
					// }
					// logger.info('vm: ' + voteMembers);
					// logger.info('-----------------------------');
					// for (var gay in voteMembers) {
					// 	logger.info('gay.id: ' + gay.id);
					// }
					// logger.info(util.inspect(voteMembers, false, null))
					


					//var channels = bot.channels;
					//var initiator = {userID : userID};
					//bot.getUser(initiator, logger.info);

					// for (var c in channels) {
					// 	var channel = channels[c];
					// 	logger.info('channel: ' + channel.name + ' c-ID: ' + c);
					// 	for (var m in channel.members) {
					// 		var member = channel.members[m];

					// 		if (member.user_id === userID) {
					// 			kickChannel = {id : c, name : channel.name};
					// 			break;
					// 		}



					// 		var propVal;
					// 		for (var prop in member) {
					// 			propVal = member[prop];
					// 			logger.info(prop + ' : ' + propVal);
					// 		}
					// 		logger.info(' ');
					// 	}
                    // }
                    
                    //function get(k) {
                    //    return map[k];
                    //}